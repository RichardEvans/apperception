
%------------------------------------------------------------------------------
% Definitions for the naturals.
%------------------------------------------------------------------------------

is_given_concept(p_is_zero).
is_given_concept(p_is_one).
is_concept(p_is_zero).
is_concept(p_is_one).

permanent(isa(t_nat, obj_number_0)).
permanent(isa(t_nat, obj_number_1)).

permanent(isa(p_is_zero, obj_number_0)).
permanent(isa(p_is_one, obj_number_1)).

